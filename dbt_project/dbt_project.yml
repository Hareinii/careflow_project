name: 'careflow'
version: '1.0.0'
config-version: 2

profile: 'careflow'

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  careflow:
    staging:
      +materialized: view
    marts:
      +materialized: table
